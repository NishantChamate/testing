version: 1.0
runtime: python3.9  # Change as per your application (e.g., nodejs18, go1, etc.)
build:
  commands:
    - pip install -r requirements.txt  # Change as per your project
run:
  command: gunicorn -w 4 -b 0.0.0.0:8000 app:app  # Modify for your framework
